export default class MovieCard {
  constructor(title, description, icon, color) {
    this.title = title;
    this.description = description;
    this.icon = icon;
    this.color = color;
  }
}

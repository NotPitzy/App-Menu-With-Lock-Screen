import WeatherBox from "../models/WeatherBox.js";

const weather = [
    new WeatherBox(29, "sun"),
    new WeatherBox(26, "sun and cloud"),
    new WeatherBox(26, "cloud"),
    new WeatherBox(18, "rain"),
    new WeatherBox(15, "storm"),
    new WeatherBox(28, "sun"),
    new WeatherBox(21, "cloud"),
]

export default weather